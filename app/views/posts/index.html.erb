<h2><br><br>Simple Blogs Posts (<%= @posts.count %>)</h2>
<!--posts variable passed needs to be looped
NOTE: Notice that <% %> and not <%= %> (No '=' sign)
because we aren't outputting some kind of variable-->

<% @posts.reverse_each do |post| %>
    <div class="card">
        <div class="card-body">
            <h3 class="card-title"><%= post.title %></h3>
            <p class="card-text"><%= post.body %></p>
            <%= link_to  "Read More", post_path(post), :class => 'btn btn-outline-primary' %>
        </div>
    </div>
<% end %>
